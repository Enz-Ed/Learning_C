#include  <stdio.h>
 
int main() {
    double A;
    scanf("%lf", &A);
    
    double B;
    scanf("%lf", &B);
    
    double MEDIA;
    MEDIA = (A * 3.5 + B * 7.5) / (3.5 + 7.5);
    printf("MEDIA = %.5lf\n", MEDIA);
    
    return 0;
}
