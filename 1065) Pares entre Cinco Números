#include <stdio.h>
 
int main(){
    int x, aux, par;
    par=0;
    for(aux=0;aux!=5;aux++){
        scanf("%d", &x);
        if(x%2==0)
            par++;
    }
    printf("%d valores pares\n", par);
    return 0;
}
