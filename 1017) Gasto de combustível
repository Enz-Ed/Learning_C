#include <stdio.h>
 
int main() {
    int time, speed;
    scanf("%d", &time); scanf("%d", &speed);
    
    double fuel;
    fuel = time * speed / 12.0;
    printf("%.3lf\n", fuel);
    
    return 0;
}
