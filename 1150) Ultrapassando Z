#include <stdio.h>
 
int main() {
    int X, Z, soma, count;
    scanf("%d", &X);
    count=0;
    
	  do{
        scanf("%d", &Z);
    }while(Z<X||Z==X);
    
    for(soma=X;soma<Z;count+=1)
    	soma = X+((X+1)*count);
      
	  printf("%d\n", count-1);
	  return 0;
}
