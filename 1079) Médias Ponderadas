#include <stdio.h>
 
int main() {
    int N, aux; float f1, f2, f3, media;
    scanf("%d", &N);
    for(aux=0;aux!=N;aux++){
        scanf("%f %f %f", &f1, &f2, &f3);
        media = (f1*2+f2*3+f3*5)/(2+3+5);
        printf("%.1f\n", media);
    }
    return 0;
}
