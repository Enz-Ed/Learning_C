#include <stdio.h>

int main(){
    int N, i;
    scanf("%d", &N);
    for(i=1; i<=N+1; i++){
        if(N%i==0){
            printf("%d\n", i);
        }
        if(i==N/2){
            i+=((N/2)-1);
        }
    }
    return 0;
}
