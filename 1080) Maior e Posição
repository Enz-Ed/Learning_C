#include <stdio.h>
 
int main() {
    int x, aux, maior, input;
    maior=0; input=0;
    for(aux=0;aux!=100;aux++){
        scanf("%d", &x);
        if(x>=maior){
            maior=x;
            input=aux+1;
        }
    }
    printf("%d\n%d\n", maior, input);
    return 0;
}
