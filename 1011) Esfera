#include <stdio.h>

int main() {
    double radius;
    scanf("%lf", &radius);
    
    double pi;
    pi = 3.14159;
    
    double VOLUME;
    VOLUME = (4/3.0 * pi * radius * radius * radius);
    printf("VOLUME = %.3lf\n", VOLUME);
    
    return 0;
}
