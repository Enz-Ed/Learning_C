#include <stdio.h>

int main()
{
    int x, X[10];
    int i;
    
    for (i=0; i<10; i++){
        scanf("%d", &x);
        if(x<=0){
            x = 1;
        }
        X[i] = x;
    }
    
    for(i=0; i<10; i++){
        printf("X[%d] = %d\n", i, X[i]);
    }
    return 0;
}
