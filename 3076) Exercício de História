#include <stdio.h>

int main(){
    int N, count, sec;
    while(scanf("%d", &N)!=EOF){
        sec=0;
        if(N%100==0)
           sec=(N/100);
        else
           sec=(N/100)+1;
        printf("%d\n", sec);
    }
    return 0;
}
